import categories from "./schemas/categories";
import heroImages from "./schemas/heroImages";
import product from "./schemas/product";

export const schema = {
  types: [product, categories, heroImages],
};
